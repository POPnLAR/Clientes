# Ignorar todos los PDFs generados
auditoria_*.pdf
__pycache__/
*.csv.bak

# Secrets locales (nunca deben subirse al repo)
.streamlit/secrets.toml
.env

# Estado runtime del agente conversacional (vive en el VPS, no en git)
*.db
agent_service.log
alert_status_agente.json

# Config local de Claude Code (no es parte del proyecto para el equipo/CI)
.claude/
# QR temporal generado por evo_qr.py
evo_qr.png
.env.v2

# Notas locales con datos de infraestructura (repo publico)
rippling-strolling-diffie.md
.env.serp

# Entregas semanales al vendedor (datos personales de prospectos) y credenciales de esa herramienta
entregas/
.env.entrega
